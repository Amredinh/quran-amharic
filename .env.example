# Set the secret key required to authenticate into the separate HTML admin workstation (/admin)
# Default is 'admin123' if undefined. Specify a secure passphrase in your actual .env
ADMIN_SECRET_KEY=
